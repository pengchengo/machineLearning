{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: Current File",
            "type": "debugpy",
            "request": "launch",
            "cwd": "${workspaceFolder}\\mtcnn",
            "program": "${file}",
            "console": "integratedTerminal"
        }
    ]
}