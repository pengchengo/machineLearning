{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: Current File",
            "type": "debugpy",
            "request": "launch",
            "cwd": "${workspaceFolder}\\ppo\\handwrite",
            "program": "${file}",
            "console": "integratedTerminal"
        }
    ]
}